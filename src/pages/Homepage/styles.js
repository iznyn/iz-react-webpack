/**
 * pages/Tournaments/MainCustom/styles.js
 */
import { makeStyles } from '@material-ui/core/styles';
// import { poaf, media } from '@csx';
const config = {
  root: {
    position: 'relative',
  },
};

export default makeStyles(config, { name: 'Tournaments' });
